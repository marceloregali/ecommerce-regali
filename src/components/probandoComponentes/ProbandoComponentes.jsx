import Button from "@mui/material/Button";

const ProbandoComponentes = () => {
  return (
    <div>
      <Button variant="outlined">Contained</Button>
    </div>
  );
};

export default ProbandoComponentes;
